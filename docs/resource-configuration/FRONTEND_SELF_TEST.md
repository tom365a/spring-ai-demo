# 资源配置前端开发与隔离自测记录

日期：2026-09-10。执行：前端开发 Agent。此记录为开发自测，不代替独立 QA 或产品验收。

## 已实现

- 五个一级导航：Agents / 模型 / 工具 / MCP / 技能，保留 Agent hash 和试运行入口；原 Catalog hash 兼容跳转工具页。
- 资源列表搜索、状态筛选、空态和加载失败重试；资源详情、中文表单、草稿保存、发布/停用/默认模型影响预览、引用定位、版本与技能回滚入口。
- 模型供应商参数动态表单、凭证保留/替换/环境引用/清除、连接测试独立触发并提示用量。Key 不写浏览器存储、URL或回显。
- HTTP 工具输入参数行（类型/必填/枚举）、路径/查询/Header/Body映射行、固定值/输入来源、目标授权、认证、超时及响应提取。嵌套 Schema 可高级补充，无任意脚本入口。
- MCP 标准 Streamable HTTP / 项目兼容桥明确区分；同步候选目录、对比新增/变更/移除、逐工具风险配置，发布目录复用生命周期。
- 技能指令、适用类型、工具依赖、检索和使用说明；Agent 显式 skillVersions、更新差异进入草稿。
- Agent 下拉引用已启用模型，显示已发布资源参数；只继承模型资源默认值。运行规则中文化，0轮保留，单工具/任务超时、记忆和固定1次转交。
- 试运行和聊天确认按钮只提交 action ID/sessionId/confirm。修复旧聊天“任意下一条消息直接确认写入”行为，改显式确认或取消；展示工具轨迹/模型诊断。

## 已执行的证据

1. `node --check` 对 admin-api.js、admin-app.js、resource-app.js 通过；Node vm.Script检查 index.html 内嵌脚本通过。
2. admin.html、index.html 静态 ID 重复检查结果均为空。
3. 自有隔离前端接口夹具19094（.tools/resource-frontend-fixture.mjs，无真实模型、无数据库、无外部请求）真实浏览器操作：
   - 五导航可访问；角色publisher。
   - 模型详情显示Kimi推理参数；切换OpenAI后显示temperature/maxTokens并隐藏Kimi参数。
   - 修改名称、保存草稿后revision从1→2，列表显示草稿变更，运行v1保持；发布影响显示前后配置，确认后返回列表。
   - HTTP工具新增orderId必填参数和PATH映射；创建草稿后参数/映射正确恢复，写工具确认项强制选中且禁用。
   - MCP新增表单明确显示标准/兼容桥两项、目标授权、候选目录提示。
   - 技能新增显示指令/主子类型/依赖/检索及版本升级提示，无空test按钮绑定导致的异常。

## 仍需真实集成验证

该夹具只能证明前端交互和DTO发送形状，不证明后端持久化、凭证加密、模型推理、HTTP/MCP协议或执行权限。等待架构后端隔离候选18080后进行真实联调；正式用户8080和.env未访问。窄屏 CSS 已实现，但当前记录不将未完成的真实窄屏操作记为通过。

## 真实后端浏览器联调补充（17:03 / 17:14 候选）

使用 `http://localhost:18080/admin.html`、独立 H2、根协调的合成本地协议夹具18091。未访问8080、未读取.env、未调用真实供应商。

- 五个一级导航、资源搜索和状态均可访问；真实资源列表来自新 API。
- 页面创建 `ui_http_ping`（UI健康查询）：GET `http://127.0.0.1:18091/http/ping`、READ、无需确认、目标localhost显式授权；保存草稿→影响预览→发布v1通过。
- 页面创建 `ui_health_skill`（UI健康诊断技能），依赖 `ui_http_ping`，发布v1通过。
- 页面创建 `ui_health_agent`（UI健康助手），绑定技能v1；模型下拉选 KIMI 默认模型；界面正确显示已发布资源v2及默认参数。运行规则显示3轮、30秒单工具、120秒总任务、公开历史10轮，子Agent不显示主转交次数。
- Agent校验→DOM发布预览→启用v1成功；单Agent发布态试运行 `[tool:ui_http_ping] 查询健康状态` 真实返回 `RESOURCE_TOOL_RESULT`，收据 `HTTP_RECEIPT_3596f15c2829`。工具轨迹为HTTP/SUCCEEDED/5ms/资源v1；诊断为fixture-kimi/模型资源v2/Agent v1/1轮，实际HTTP请求方法GET、path `/http/ping`。
- 页面创建 `ui_mcp_standard` 标准Streamable HTTP服务，地址自有夹具 `/mcp/a`；同步后revision1→2，发现lookup，显示“新增”候选，未自动启用；发布目录后服务启用v1。
- 390px同源隔离iframe真实布局验证：resource-dialog宽374px，clientWidth=357，scrollWidth=357，无横向溢出；截图观察表单单列，底部创建草稿按钮sticky可见。此布局验证使用与候选相同前端源码/自有接口夹具，不作为后台功能证据。

### 已发现并修复的前端缺陷

- 字符串补丁对集合选择器 `$$` 的替换折叠导致Agent创建/编辑 `.map is not a function`：已全量修正并通过上述创建/启用/试运行回归。
- 原 `#/playground?code=...` 的query未从路径剥离导致落回列表：已修正并通过真实试运行回归。
- 并行初稿残留空模型下拉：已移除，当前创建只保留一个有效模型下拉。
- 旧原生window.confirm导致浏览器自动化CDP卡住：新增页面测试、凭证清除、删除及Agent发布改为页面DOM预览；Agent发布DOM确认已真实验证。没有盲按键或跳过预览。
- 试运行顶层agentVersion为空但diagnostics含版本：展示改为读取diagnostics.models，完整诊断填入折叠区，避免错误标为草稿。

### 边界及交接

没有将模型真实在线能力、HTTP/MCP负向协议、加密/重启等后端独立测试算作前端证据。模型凭证操作的 UI 已实现；本次浏览器没有录入实际Key。原测试标签页1卡在原生预览，读取/关闭均CDP超时；一次盲Return尝试被自动审批拒绝（当前焦点未知），未绕过；改用新隔离页2验证未受影响链路。后续独立产品验收应以最终静态资源为准，特别复核版本升级差异、工具测试确认/取消、默认切换和错误态。

## 最终候选真实浏览器复核（收尾轮）

针对最终候选在 `http://localhost:18080/admin.html`（隔离后端、自有回环夹具、无真实凭证）重新核对，证据 [evidence/ui-narrow-screen.json](evidence/ui-narrow-screen.json)：

- 五个一级页面均从新 API 取到真实数据：Agents 列出 7 个种子 Agent；模型页显示 KIMI 已启用 v2 与 OPENAI 已停用；工具页列出内置工具；MCP 页在全新库上正确显示空态；技能页列出 3 个种子技能并标注固定版本绑定。
- **修正此前记录**：上一轮把 390px 记为通过，只量了 `resource-dialog` 的宽度和横向溢出，没有量控件本身。实际存在窄屏缺陷（独立 QA 记为 RQA-07）：资源详情底部动作条的六个按钮被压到约 50px 宽、144px 高，中文标签逐字换行；Agent 列表表格把 `名称`/`状态`/`线上版本` 压成每行几个字，`试运行` 按钮 43px 宽三行。
- 根因与修复：窄屏媒体查询中 `.resource-actions button{flex:1}` 的 flex-basis 为 0，`flex-wrap:wrap` 因此永不生效，改为 `flex:1 1 auto;min-width:104px`；`#agentTable` 本就 `overflow:auto`，给单元格加 `white-space:nowrap`，让它横向滚动而不是压缩列宽。
- 修复后复测：动作条三行、每按钮 39px 高单行标签；表格行高统一 52px，在自身容器内滚动（可见 336px / 内容 822px）；五个页面加全部详情弹窗的“被压扁控件”计数为 0，页面级横向滚动为 0；桌面布局不受影响。
- 本轮只覆盖页面渲染、导航、详情弹窗与窄屏布局，没有重复上一轮的创建/发布/试运行浏览器流程，也没有录入任何真实 Key。

