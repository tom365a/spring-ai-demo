# 项目未完成功能盘点与后续工作

检查日期：2026-09-10。此表来自源代码检查；不把 Demo 设计边界直接等同于已复现缺陷。本轮 Agent 配置交付以 PRD 和最终测试、验收报告为准。

## 本轮必须完成

管理页完整创建、可信工具/技能目录及实际应用、仅主 Agent 关联子 Agent、启用时发布并动态加载、自定义主 Agent 正式聊天入口、草稿与运行版本隔离、停用过滤、配置持久化与重启恢复、独立测试和产品验收。原始缺口及责任排期见 PROJECT_PLAN.md，逐项门禁见 PRD.md。

## 后续待办

| 能力 | 检查证据与当前边界 | 建议下一步 |
| --- | --- | --- |
| 真实订单、工单、补偿业务 | OrderTicketTools 和 DefectCompensationTools 使用内存 Mock 数据，重启不保留业务操作；不等同于真实企业接口 | 定义业务系统适配接口、鉴权、幂等和错误契约，再替换 Mock 并做契约测试 |
| 正式身份认证与审计 | AdminAgentController 信任客户端 X-Admin-Role；页面可以选择角色。现有角色检查只适用于受控演示 | 上线前接入服务端身份认证与授权、配置变更审计、访问边界；当前交付不宣称生产鉴权 |
| 多实例配置同步 | 运行注册表为进程内状态 | 增加版本事件广播/轮询和实例版本监控，定义故障补偿与一致性边界；本轮验证单实例 |
| 完整 MCP 协议与可管理服务目录 | knowledge-mcp/server.py 提供 /tools、/tools/call HTTP 桥；/sse 仅发 endpoint 与 ping，未实现对应 /message 协议处理 | 如需对接标准 MCP 客户端，使用正式协议实现并做协议兼容测试；HTTP 桥仍可单独使用 |
| 模型高级设置完整执行 | 初始代码 modelConfig 及 outputSchema 多为存储配置，不能从存在字段推断运行生效 | 模型参数以本轮最终设计/测试为准；结构化输出 schema 强制约束仍需独立模型协议验证 |
| Agent 级记忆及试运行清理 | 初始 SessionService 使用全局窗口；trialSessionTtlMinutes 存在配置但未见定时清理执行 | 明确每 Agent 窗口语义，补过期试运行会话清理及保留策略 |
| 工具轮次控制与超时预算 | 初始 maxToolRounds 是配置字段，未形成可验证的统一执行预算 | 在工具调用器实现轮次、总时长、失败重试上限和取消传播，补异常模型测试 |
| 对话工具轨迹汇总 | 本轮真实工具回调成功，但 ChatResponse.toolCalls 对自动 ToolCallback 往返仍返回空数组 | 汇总实际工具执行事件、耗时与结果到会话响应及审计；当前可通过受控模型请求和工具日志验证调用 |
| 知识库文件持久化可观测性 | SimpleFileVectorStore 的文件加载/保存异常被忽略 | 增加失败日志、健康状态和可恢复写入；验证磁盘满、权限和损坏文件场景 |
| 知识样例导入幂等性 | 聊天页初始化会调用 knowledge/bootstrap，bootstrapSamples 再次 ingest 同一来源；页面联调中样例记录由3条增至6条 | 为来源和内容版本增加去重/更新规则，避免页面重复加载累计重复知识片段 |
| 真实模型与外部服务效果验收 | 本地受控模型能验证提示词、工具协议和动态路由，不能证明远程模型回答质量或 MCP 可用性 | 配置有效服务后增加真实模型、多模态、RAG 引用质量与远端 MCP 集成验收 |

这些事项不作为本轮已完成能力；其中生产接入事项应在将 Demo 对外服务前落实。后续排期应根据具体部署目标与业务系统接口重新评估，不将本轮测试结论扩展为整个生产系统无缺陷保证。
